name: Export MS Blocks 2346

on:
  workflow_dispatch:   # lets you trigger it manually from the Actions tab

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run export
        env:
          VC_SCHOOL_ROUTE: ${{ secrets.VC_SCHOOL_ROUTE }}
          VC_CLIENT_ID: ${{ secrets.VC_CLIENT_ID }}
          VC_CLIENT_SECRET: ${{ secrets.VC_CLIENT_SECRET }}
        run: python export_ms_blocks_2346.py --start 2026-08-01 --end 2027-06-30 --out ms_blocks_2346.csv

      - name: Upload result
        uses: actions/upload-artifact@v4
        with:
          name: ms-blocks-2346
          path: ms_blocks_2346.csv
